import "./globals.css";

export const metadata = {
  title: "AP Aging Dashboard",
  description: "Accounts Payable Aging — drag-drop invoices, zero extraction cost",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <link
          href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap"
          rel="stylesheet"
        />
      </head>
      <body>{children}</body>
    </html>
  );
}
